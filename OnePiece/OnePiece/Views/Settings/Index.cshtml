@model IEnumerable<OnePiece.Models.Setting>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FruitCountPerPage)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WeaponCountPerPage)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PirateGroupCountPerPage)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PersonCountPerPage)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FruitCountPerPage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WeaponCountPerPage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PirateGroupCountPerPage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PersonCountPerPage)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
